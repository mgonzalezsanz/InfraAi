# Copy to infrai.config.yaml in the target repo. Read by the Security/cost agent —
# not committed to InfraAI itself, config stays out of the UI/agent code.

budget_ceiling_usd_per_month: 100

allowed_resource_types:
  - aws_s3_bucket
  - aws_s3_bucket_versioning
  - aws_s3_bucket_lifecycle_configuration
